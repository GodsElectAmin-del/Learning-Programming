import numpy as np
def simulate_dice_rolls(um_rolls):
  i = um_rolls
  if um_rolls == 1:
    return 1
  return  (np.random.randint(1, 6)/i) * (simulate_dice_rolls(um_rolls -1)/i)
print(simulate_dice_rolls(4))