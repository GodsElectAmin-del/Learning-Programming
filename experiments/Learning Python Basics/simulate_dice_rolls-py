import numpy as np 
def simulate_dice_rolls(um_rolls):
  average_value = 1
  i = 0
  if um_rolls == 0:
    return (average_value/4)
  return  (np.random.randint(1, 6) * simulate_dice_rolls(um_rolls -1))
print(simulate_dice_rolls(4))

